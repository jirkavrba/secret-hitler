namespace SecretHitler.Engine
{
    /// <summary>
    /// Enum representing the political party in the game
    /// Also this enum is used to represent policies, membership and everything else related here
    /// </summary>
    public enum Party
    {
        Fascist,
        Liberal,
        Hidden
    }
}